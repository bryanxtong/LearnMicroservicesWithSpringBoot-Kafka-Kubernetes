spring:
  application:
    name: gateway
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          - name: application
          - name: ${spring.application.name}
        namespace: microservices
      client:
        namespace: microservices
      reload:
        enabled: true
        mode: polling
        period: 15000ms
