spring:
  application:
    name: gateway
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          - name: ${spring.application.name}
          - name: application
        namespace: microservices
      client:
        namespace: microservices
      reload:
        enabled: true
        mode: polling
        period: 15000ms
